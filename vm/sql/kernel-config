# TEAM_018: Guest kernel config for PostgreSQL VM
# TEAM_023: Full Field Guide Section 2.2 compliance
#
# Reference: "A Field Guide to Deploying Self-Hosted Services on Android 16 with AVF"

# VIRTIO transport (REQUIRED for all virtual devices)
CONFIG_VIRTIO=y
CONFIG_VIRTIO_PCI=y

# Virtual block device (root filesystem)
CONFIG_VIRTIO_BLK=y

# Virtual networking
CONFIG_VIRTIO_NET=y

# Host-guest communication channel
CONFIG_VIRTIO_VSOCKETS=y

# Entropy source (prevents SSH/TLS hangs)
CONFIG_HW_RANDOM=y
CONFIG_HW_RANDOM_VIRTIO=y

# PostgreSQL shared memory
CONFIG_SYSVIPC=y

# Disable Android-specific features (reduces attack surface)
# CONFIG_ANDROID_BINDER_IPC is not set

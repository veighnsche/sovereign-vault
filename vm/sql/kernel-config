# TEAM_018: MINIMAL kernel config for PostgreSQL VM
#
# ONLY enable what's strictly required:
# - VIRTIO_NET: VM networking
# - SYSVIPC: PostgreSQL shared memory (shmget)
#
# DO NOT ADD:
# - Netfilter/iptables (tailscale serve doesn't need it)
# - MARK targets (not needed with userspace networking)
# - Any other "just in case" features
#
# Stock GKI kernel lacks SYSVIPC, so we must build minimal custom kernel.

# VM networking
CONFIG_VIRTIO=y
CONFIG_VIRTIO_NET=y

# PostgreSQL shared memory (REQUIRED - shmget fails without this)
CONFIG_SYSVIPC=y

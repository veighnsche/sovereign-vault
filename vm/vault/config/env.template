# Vaultwarden Environment Configuration
# TEAM_034: This is a TEMPLATE - actual values are set at runtime in init.sh
#
# See: https://github.com/dani-garcia/vaultwarden/wiki/Configuration-overview

# ============================================================================
# Database (PostgreSQL via internal bridge network)
# ============================================================================
# SQL VM is at 192.168.100.2 on the internal bridge
DATABASE_URL=postgresql://vaultwarden:CHANGE_ME@192.168.100.2:5432/vaultwarden

# ============================================================================
# Domain and HTTPS
# ============================================================================
# IMPORTANT: This is updated dynamically by init.sh with actual Tailscale hostname
DOMAIN=https://sovereign-vault.tail5bea38.ts.net

# Rocket (web server) settings
ROCKET_PORT=443
ROCKET_ADDRESS=0.0.0.0
ROCKET_TLS={certs="/data/vault/tls/cert.pem",key="/data/vault/tls/key.pem"}

# ============================================================================
# Security
# ============================================================================
# Disable signups after you create your account!
SIGNUPS_ALLOWED=true

# Admin panel token (generate with: openssl rand -base64 48)
# ADMIN_TOKEN=CHANGE_ME

# ============================================================================
# Data Storage
# ============================================================================
DATA_FOLDER=/data/vault/data
WEB_VAULT_FOLDER=/data/vault/web-vault
WEB_VAULT_ENABLED=true

# ============================================================================
# Logging
# ============================================================================
LOG_LEVEL=info
EXTENDED_LOGGING=true

# ============================================================================
# Optional Features
# ============================================================================
# WebSocket notifications (for browser extension sync)
WEBSOCKET_ENABLED=true
WEBSOCKET_ADDRESS=0.0.0.0
WEBSOCKET_PORT=3012

# Send emails (disabled by default - configure SMTP if needed)
SMTP_HOST=
SMTP_FROM=
